{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Feuille</h1>

    {{ include('feuille/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('feuille_index') }}">back to list</a>

    {{ include('feuille/_delete_form.html.twig') }}
{% endblock %}
