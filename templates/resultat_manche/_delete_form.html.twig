<form method="post" action="{{ path('resultat_manche_delete', {'id': resultat_manche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resultat_manche.id) }}">
    <button class="btn">Delete</button>
</form>
